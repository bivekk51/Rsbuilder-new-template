/*
 *
 * App model transformers
 *
 * Bidirectional API ↔ UI data mapping functions
 * For now, App container has minimal transformation needs
 * Add transformers here as needed for API responses
 */

/**
 * Transform API response to UI format
 */
export const transformAppDataFromAPI = (data: any): any => {
  // Add transformation logic as needed
  return data;
};

/**
 * Transform UI data to API format
 */
export const transformAppDataToAPI = (data: any): any => {
  // Add transformation logic as needed
  return data;
};
