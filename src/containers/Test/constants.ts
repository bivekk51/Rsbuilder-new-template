/*
 *
 * Test constants
 *
 */

const constantKey = 'app/containers/Test';

const testConstants = {
  DEFAULT_ACTION: `${constantKey}/DEFAULT_ACTION`,
  DEFAULT_ACTION_SUCCESS: `${constantKey}/DEFAULT_ACTION_SUCCESS`,
  DEFAULT_ACTION_ERROR: `${constantKey}/DEFAULT_ACTION_ERROR`,
};

export default testConstants;
